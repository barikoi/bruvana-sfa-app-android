<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/_10sdp">

    <ImageView
        android:id="@+id/ivClose"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/close"
        android:src="@drawable/ic_close"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvGiftName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/_10sdp"
        android:text="Gift Name"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@+id/llCardCounter"
        app:layout_constraintEnd_toStartOf="@+id/llCardCounter"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/llCardCounter" />

    <LinearLayout
        android:id="@+id/llCardCounter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:background="@drawable/cardview_bg_stroke_2dp"
        android:orientation="horizontal"
        android:padding="1dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/ivClose">

        <ImageButton
            android:id="@+id/btnMinus"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:background="@drawable/cardview_bg_stroke_2dp"
            android:backgroundTint="@color/btn_gray_light"
            android:contentDescription="@string/remove"
            android:gravity="center_vertical"
            android:padding="8dp"
            android:src="@drawable/ic_minus"
            app:tint="@color/colorPrimaryDark" />

        <EditText
            android:id="@+id/tvCount"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:autofillHints="NO"
            android:background="@null"
            android:ems="4"
            android:gravity="center"
            android:inputType="number"
            android:text="@string/_0"
            android:textColor="@color/black"
            android:textSize="12sp"
            android:textStyle="bold"
            tools:ignore="LabelFor" />

        <ImageButton
            android:id="@+id/btnPlus"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:background="@drawable/cardview_bg_stroke_2dp"
            android:backgroundTint="@color/btn_gray_light"
            android:contentDescription="@string/add"
            android:gravity="center_vertical"
            android:padding="8dp"
            android:src="@drawable/ic_plus"
            app:tint="@color/colorPrimaryDark" />

    </LinearLayout>

    <TextView
        android:id="@+id/tvImages"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Images"
        android:textColor="@color/black"
        android:textStyle="bold"
        android:layout_marginTop="@dimen/_10sdp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/llCardCounter" />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/_20dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tvImages">

        <include
            android:id="@+id/llImagePickerView"
            layout="@layout/layout_image_picker" />

    </androidx.constraintlayout.widget.ConstraintLayout>


</androidx.constraintlayout.widget.ConstraintLayout>